services:
  postgres-db:
    build: ./data
    volumes:
      - ./data/pgdata:/var/lib/postgresql/data/data
    ports:
      - 7557:5432
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=rusalka-dev
      - PGDATA=/pgdata
